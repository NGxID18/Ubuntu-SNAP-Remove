set chmod +x setup.sh before run or set executeable as program on properties

Key value for network devices (wlan0, enp5s0, eth0, etc) need to be manually adjust based on your own unique interface name
